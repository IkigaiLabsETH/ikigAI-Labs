import type { NextPage } from "next";
import AudioGeneratorPage from "@/templates/AudioGeneratorPage";

const AudioGenerator: NextPage = () => {
    return <AudioGeneratorPage />;
};

export default AudioGenerator;
