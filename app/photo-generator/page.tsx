import type { NextPage } from "next";
import PhotoGeneratorPage from "@/templates/PhotoGeneratorPage";

const PhotoGenerator: NextPage = () => {
    return <PhotoGeneratorPage />;
};

export default PhotoGenerator;
